source "http://rubygems.org"

gem "warden", "~> 1.0.3"

########
# The gems below here are not explicitly required or loaded.
# Requires happen inside of each given module (if gem is needed)
# 
# TODO: Figure out if we want to do this, or handle loading differently
########
# gem "rails", ">= 3.0.0", :require => false

# Add dependencies to develop your gem here.
# Include everything needed to run rake, tests, features, etc.
group :development do
  gem "rake", ">= 0"
  gem "rspec", "~> 2.5.0"
  gem "yard", "~> 0.6.0"
  gem "bundler", "~> 1.0.10"
  gem "jeweler", "~> 1.5.2"
  gem "rcov", ">= 0"
end
